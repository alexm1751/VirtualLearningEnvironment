{% extends 'tableLayout.html.twig' %}
{% block content %}

    {% include 'navigation.html.twig' %}

    {% include 'ad_adminNav.html.twig' %}
    <!-- Page Content Holder -->
    <div id="content">
        <ul class="breadcrumb">
            <li>
                <a href="{{ home }}">Home</a>
                <i class="icon-angle-right"></i>
            </li>
            <li>
                <a href="{{ user_edit }}">User Edit</a>
                <i class="icon-angle-right"></i>
            </li>

        </ul>

        {% if users is null %}
            <p>There is no user data to display at this time.</p>
        {% else %}
            <table id="userTable" class="table table-striped table-bordered bootstrap-datatable datatable" cellspacing="0" width="100%">
                <thead>
                <tr>
                    <th>UserID</th>
                    <th>Email</th>
                    <th>Full Name</th>
                    <th>Address</th>
                    <th>Number</th>
                    <th>Gender</th>
                    <th>Rank</th>
                    <th>View/Edit/Delete</th>

                </tr>
                </thead>
                <tfoot>
                <tr>
                    <th>UserID</th>
                    <th>Email</th>
                    <th>Full Name</th>
                    <th>Address</th>
                    <th>Number</th>
                    <th>Gender</th>
                    <th>Rank</th>
                    <th>View/Edit/Delete</th>
                </tr>
                </tfoot>
                <tbody>
                {% for user in users %}
                    <tr>

                        <td>{{ user.dbUniqueID }}</td>
                        <td>{{ user.dbEmail }}</td>
                        <td>{{ user.dbFullName }}</td>
                        <td>{{ user.dbAddress }}</td>
                        <td>{{ user.dbNumber }}</td>
                        <td>{{ user.dbGender }}</td>
                        <td>{{ user.dbRank }}</td>
                        <td>
                            <div class='btn-group' role='group' aria-label='...'>
                                <a href="#view<?php echo $id;?>" data-toggle="modal"><button type='button' class='btn btn-primary btn-sm'><span class='glyphicon glyphicon-eye-open' aria-hidden='true'></span></button></a>
                                <a href="#edit<?php echo $id;?>" data-toggle="modal"><button type='button' class='btn btn-warning btn-sm'><span class='glyphicon glyphicon-edit' aria-hidden='true'></span></button></a>
                                <a href="#delete<?php echo $id;?>" data-toggle="modal"><button type='button' class='btn btn-danger btn-sm'><span class='glyphicon glyphicon-trash' aria-hidden='true'></span></button></a>
                            </div>
                        </td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
            <form>

                <br>
                <br>
                <div class="form-group">
                    <a href="#edit<?php echo $id;?>" data-toggle="modal">Create New User: <button type='button' class='btn btn-success btn-sm'><span class='glyphicon glyphicon-plus' aria-hidden='true'></span></button></a>
                </div>
            </form>
        {% endif %}


    </div>
    </div>

    {% include 'footer.html.twig' %}
{% endblock %}